server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/votesdb
    username: root
    password: null

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect